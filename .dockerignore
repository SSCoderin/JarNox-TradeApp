# Node dependencies (reinstalled inside container)
node_modules

# Build output (only needed for production, not for build stage)
dist
build

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment files (don’t bake secrets into image)
.env
.env.local
.env.*.local

# OS / Editor junk
.DS_Store
*.swp
*.swo
*.tmp

# Git / VCS
.git
.gitignore
